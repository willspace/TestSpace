extends ../layout

block content
	.container
		.row
			form.form-horizontak(method="post",action="/admin/movie/category/save")
				input(type="hidden",name='category[_id]',value=category._id)
				.form-group
					label.col-sm-2.control-label(for="inputCategory") 电影分类
					.col-sm-10
						input#inputCategory.form-control(type="text", name="category[name]", value=category.name)
				.form-group
					.col-sm-offset-2.col-sm-10
						button.btn.btn-default(type="submit") 录入